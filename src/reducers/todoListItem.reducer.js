// Initial State
export const initialState = {
  id: null,
  description: '',
  isComplete: false,
  posiiton: 0,
  categories: []
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
